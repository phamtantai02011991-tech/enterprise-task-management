namespace TaskManagementWeb.Models.Enums
{
    public enum ProjectStatus
    {
        Planning = 0,    // Lập kế hoạch
        Active = 1,      // Đang triển khai
        OnHold = 2,      // Tạm dừng
        Completed = 3,   // Đã hoàn thành
        Cancelled = 4    // Đã hủy
    }
}
